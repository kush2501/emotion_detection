from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small ML project is used to predict emotion by using text.',
    author='kush',
    license='',
)
